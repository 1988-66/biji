1. 初始状态：B_key_val=0（按键未按），B_key_processed=0（解锁） 2. 用户按下B键：B_key_val=1 - 主循环检测到 `1 && 0`，条件成立 - 执行C_FLAG自增等逻辑，然后设置B_key_processed=1（上锁） 3. 按键保持按下（或抖动）：B_key_val=1 - 主循环检测到 `1 && 1`，条件不成立，不执行任何逻辑 4. 用户松开B键：B_key_val=0 - 触发 `if(B_key_val == 0)`，设置B_key_processed=0（解锁） 5. 等待下一次按键，重复上述流程