1.
PSC 越大，计数到最大值越慢。
2.
四驱小车的 4 路 PWM 建议使用同一个定时器的不同通道（或同总线的定时器），保证 PWM 频率一致，电机转速同步性更好。
3.
PWM：
`TIM_OCxPreloadConfig(TIMx, TIM_OCPreload_Enable)`;
//预装载功能配置（影子寄存器机制，核心保障调速平稳）（x=1~4，对应通道 1~4）
TIM_CtrlPWMOutputs(TIMx, ENABLE);
//*专门用于使能 STM32 “高级定时器”（TIM1、TIM8、TIM12 等）的 PWM 输出功能，
//解锁定时器的 “刹车 / 死区” 功能模块，允许 PWM 信号从 GPIO 引脚输出**。*
4.

