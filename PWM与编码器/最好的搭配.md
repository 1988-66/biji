1.
PWM 最优参数：F103（PSC=3，ARR=599）、F407（PSC=3，ARR=999），编码器固定（PSC=0，ARR=65535）；
2.
PSC 越大，计数到最大值越慢。
3.
四驱小车的 4 路 PWM 建议使用同一个定时器的不同通道（或同总线的定时器），保证 PWM 频率一致，电机转速同步性更好。
4.
占空比 = CCRx / (ARR+1)，如果使用Motor_SetSpeedD（i)，ARR=999，i的范围不能在0-100，占空比最大为10%，太小了。
5.

