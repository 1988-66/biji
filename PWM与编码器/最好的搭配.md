1.
PWM 最优参数：F103（PSC（预分频器）=71，ARR（自动重装值）=19999）、F407（PSC=83，ARR=19999），编码器固定（PSC=0，ARR=65535）；
2.
四驱小车的 4 路 PWM 建议使用同一个定时器的不同通道（或同总线的定时器），保证 PWM 频率一致，电机转速同步性更好。
3.
占空比 = CCR（输出比较寄存器）x / (ARR+1)，如果使用Motor_SetSpeedD（i)，ARR=19999，i的范围在4000以上，占空比为20%，最小启动。



